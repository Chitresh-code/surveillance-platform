meta {
  name: Update a camera
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/cameras/{{camera_id}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Lobby North (renamed)"
  }
}

assert {
  res.status: eq 200
}
