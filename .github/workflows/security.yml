name: Security

on:
  pull_request:
  push:
    branches: [main]

jobs:
  codeql:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v3
        with:
          languages: python
      - uses: github/codeql-action/analyze@v3

  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      # ponytail: continue-on-error until the repo's Dependency graph setting is turned on
      # (Settings > Security > Code security and analysis) — the action 400s without it.
      - uses: actions/dependency-review-action@v4
        continue-on-error: true
